import 'package:flutter/material.dart';

class Appcolors {
  static const primarycolor = Color(0xff236c8c);

  static const backgroundColor = Colors.white;
  static const textColor = Colors.black;
  static const sectxtcolor = Colors.white;

  static const bordercolor = Colors.grey;
  static const hintcolor = Colors.grey;

  static const favcolor = Colors.red;

  static const bottomNavColor = Colors.white;
  static const activebottomNavColor = Colors.black;
  static const inActivebottomNavColor = Colors.grey;
}
